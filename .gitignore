.DS_Store
.claude
CLAUDE.md
node_modules
supporting/tests/spec-validation

# TEMPORARY: local-only auditing test. Not yet wired into CI because the
# existing specs have many pre-existing duplicate-schema groups. We're
# working through them; once the backlog is clear (or an allowlist is in
# place), this entry should be removed and the test committed.
supporting/tests/latest/no-duplicate-schemas.test.js
